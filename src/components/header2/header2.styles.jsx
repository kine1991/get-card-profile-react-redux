import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => {
    return {
        navbar: {
            // display: 'flex'
        },
        list: {
            width: 300,
        },
        title: {
            // flexGrow: 1
        },
        switch: {
            // flexGrow: 1
        },
        fillSpace: {
            flexGrow: 1
        }
    }
})